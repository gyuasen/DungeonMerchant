# UX改善バックログ（ユーザー提案 2026-07-31）

家側でユーザーから提示された改善案17件。原文の記録が目的。

- 記録日: 2026-07-31（当初 2026-07-22 と誤記していたのを訂正）
- 提案元: ユーザー（家側）
- 注意: 本ファイルは提案の受け皿。実装に着手したら `CLAUDE_WORK_LOG.md` へ詳細を、完了時に `SHARED_PROJECT_STATUS.md` へ短く追記すること。

> **⚠ 状態の訂正（2026-07-31）**
> 当初この一覧は「全件未着手」と記録していたが、これは誤りだった。**昨日までの家側実装で大半が既に実装済み**であることが、実コード監査（Codex Sol、read-only）で判明した。
> 監査結果: **実装済み14件 / 部分実装2件（#1, #5）/ 未着手1件（#16）**。以下の一覧は監査後の正確な状態。
> なお、この監査はUI層の大規模リファクタ完了後（EditMode 794件、`SimpleMercenaryHireUI` 12 partial化）の作業コピーに対して実施している。UIリファクタの状況は `handoff/UI_REFACTOR_HANDOFF.md` が正本。

---

## 一覧（2026-07-31 実コード監査後）

| # | 改善案 | 分類 | 状態 | 根拠 |
|---|---|---|---|---|
| 1 | 新規にゲームを始めたとき施設の説明を入れる | 説明・オンボーディング | **実装済み(2026-07-31)** | 新規ゲーム(セーブ無し)時のみチュートリアル6ページを自動表示。既存セーブロード時は非表示。セーブ変更なし。`TutorialController.cs:90` / `SimpleMercenaryHireUI.cs:584` |
| 2 | 治療院で傭兵の詳細が出るようにする | 傭兵・治療・転職 | 実装済み | `HealPageUI.cs:98` 詳細ボタン→`ShowCharacterDetails` |
| 3 | 転職のさい変更先のステータスを表示する | 傭兵・治療・転職 | 実装済み | `SimpleMercenaryHireUI.HireParty.cs:420,443` プレビュー+差分 |
| 4 | 倉庫拡張の時、確認用のUIを用意する | 倉庫・装備管理 | 実装済み | `SimpleMercenaryHireUI.Economy.cs:393` 確認オーバーレイ |
| 5 | 倉庫のアイテムを一括で売却できるようにする | 倉庫・装備管理 | **実装済み(2026-07-31)** | 「売却用」分類＋環境素材6種(鉄鉱石/銀鉱石/薬草/毒消し草/堅木材/霊木材)をpersistentIdで明示対象化。制作材料・消耗品・装備は対象外。`EconomyController.cs:14,222` |
| 6 | アイテムの詳細を倉庫でカーソルを合わせると表示できるようにする | 倉庫・装備管理 | 実装済み | `InventoryPageUI.cs:123` `GenericHoverTooltipTrigger` |
| 7 | 治療院は回復が必要な傭兵のみ表示する | 傭兵・治療・転職 | 実装済み | `HealPageUI.cs:45` `needsHealingProvider` で絞り込み+空表示 |
| 8 | 1日のリザルトの文字に色を付けてわかりやすくする | 表示・演出 | 実装済み | `DailyResultController.cs:975` リッチテキスト色（UITheme集約） |
| 9 | 鍛冶屋を奥に進むほど商品が増える形にする | 市場・鍛冶屋 | 実装済み※ | `BlacksmithManager.cs:184` 町進行に応じ商品拡大。※原文「奥に進む」の意図が階層UIなら別途要確認 |
| 10 | 消耗品スロットと装備は枠をクリックして、装備できるものの中から選ぶようにする | 倉庫・装備管理 | 実装済み | `CharacterEquipmentController.cs:161` スロット選択→候補提示 |
| 11 | 装備は別の保管用枠を用意する | 倉庫・装備管理 | 実装済み | `MerchantInventory.cs:46,76` 装備は `equipmentInstances` で別管理、容量に含めない |
| 12 | 鍛冶屋や市場でも画像を表示・確認できるようにする | 市場・鍛冶屋 | 実装済み | `MarketPageUI.cs:162` / `BlacksmithPageUI.cs:156` `ResolveSprite()` |
| 13 | 従業員の言葉はゲーム起動時に一回のみでいい | 説明・オンボーディング | 実装済み | `FacilityGreetingController.cs:19,72` 起動セッション内で施設種別ごと1回 |
| 14 | 町マップの画像を町ごとに変更する | 表示・演出 | 実装済み | `WorldMapService.cs:28,182` 8町別画像+欠損時フォールバック |
| 15 | 施設UIを初めて訪れたときに説明を入れる | 説明・オンボーディング | 実装済み※ | `FacilityGreetingController.cs:24` 施設種別ごと初回に案内。※現仕様は「町別初回」でなく「起動セッション内の施設種別初回」 |
| 16 | 装備以外のアイテムの図鑑をメニューに追加 | 図鑑・メニュー | **実装済み(2026-07-31)** | メニューに「アイテム図鑑」追加。装備を除く全アイテム掲載、入手済みのみ表示・未発見は「？？？」。`BookPageUI`非改変、`ItemCodexOverlayView`+`ItemCodexPresenter`新設。発見管理はセーブ**v36→v37**へ昇格し`discoveredItemPersistentIds`追加(移行テスト付き) |
| 17 | タイトル画面に戻るボタンをメニューに追加 | 図鑑・メニュー | 実装済み | `SimpleMercenaryHireUI.UIFactory.cs:65` 「セーブしてタイトルへ」戦闘/探索中は拒否 |

**集計(2026-07-31 実装完了後): 実装済み17 / 部分実装0 / 未着手0 — 全17件対応済み**

### 2026-07-31 に実装した残3件(Sol実装・メイン監査・luna裏取り)
- **#1**: 新規ゲーム(セーブ無し)時のみチュートリアル6ページを自動表示。既存セーブロード時は非表示。施設従業員案内(#13/#15)とは発火契機が別で二重表示なし。セーブ変更なし。
- **#5**: 一括売却対象を「売却用」分類＋環境素材6種へ拡張(persistentId明示)。制作材料・消耗品・装備は対象外を維持。
- **#16**: メニューに「アイテム図鑑」新設。`ItemCodexOverlayView`+`ItemCodexPresenter`(BookPageUI非改変・View/Presenterパターン)。**セーブv36→v37**へ昇格し`discoveredItemPersistentIds`追加、`SaveDataMigrator`にv37移行ステップ＋移行テスト、倉庫追加時に非装備アイテムをpersistentIdで発見登録。

### 検証状況
- Runtimeビルド 警告0・エラー0(独立検証済み)。EditModeテストcsproj 警告0・エラー0(追加テスト含めコンパイル可)。
- luna裏取り: セーブCapture/Restore両方あり・既存発見集合を破壊しない・移行は空初期化のみ・#5範囲正確・reflection参照privateフィールド不変・#1新規時のみ、を確認。
- **未実施(要ユーザー)**: Unity Test Runnerでの全件PASS確認(特にv36→v37移行テストと追加テスト)。実機での見た目・挙動確認。

### 注記(セッション中に判明した基準の訂正)
- 実コードの`CurrentVersion`は**v37**(本実装後)。私の旧`TEST_BASELINE.md`/旧記述の「v28」は同期前の古い値。
- `TEST_BASELINE.md`のEditMode 559件も古い(現在≒794+新規)。再計測が望ましい。

---

## A. 説明・オンボーディング

### 1. 新規にゲームを始めたとき施設の説明を入れる

> 新規にゲームを始めたとき施設の説明を入れる。

新規ゲーム開始時に、各施設が何をする場所かの説明を表示する。

### 15. 施設UIを初めて訪れたときに説明を入れる

> 施設UIを初めて訪れたときに説明を入れる。

施設UIごとに、初回訪問時のみ説明を表示する。

**#1 と #15 の関係**: どちらも施設の説明表示だが、発火タイミングが異なる（新規ゲーム開始時にまとめて / 各施設の初回訪問時に個別に）。実装時にどちらの方式を採るか、あるいは併用するかを先に決めること。「初回訪問済み」フラグを持つ場合はセーブ対象になるため、セーブバージョンへの影響を確認する。

### 13. 従業員の言葉はゲーム起動時に一回のみでいい

> 従業員の言葉はゲーム起動時に一回のみでいい。

施設従業員の案内表示が施設に入るたびに出る現状を、ゲーム起動につき1回のみへ変更する。

**補足**: 「ゲーム起動時に一回」はセッション単位の抑制であり、セーブへの永続化は不要と読める。#15（初回訪問時の説明）とは別軸の要求なので、両立させる設計にすること。

---

## B. 傭兵・治療・転職

### 2. 治療院で傭兵の詳細が出るようにする

> 治療院で傭兵の詳細が出るようにする。

治療院の一覧から傭兵の詳細（ステータス等）を確認できるようにする。

### 7. 治療院は回復が必要な傭兵のみ表示する

> 治療院は回復が必要な傭兵のみ表示する。

全快の傭兵を一覧から除外し、治療が必要な傭兵のみ表示する。

**補足**: #2 と #7 は同じ治療院画面への変更なので、まとめて着手すると効率が良い。全員全快で一覧が空になる場合の表示（「治療が必要な傭兵はいません」等）も併せて決めること。

### 3. 転職のさい変更先のステータスを表示する

> 転職のさい変更先のステータスを表示する。

転職先を選ぶ際に、転職後のステータスを表示して比較できるようにする。

**補足**: 現在値との差分表示（+/-）にすると判断しやすい。

---

## C. 倉庫・売却・装備管理

### 4. 倉庫拡張の時、確認用のUIを用意する

> 倉庫拡張の時、確認用のUIを用意する。

倉庫拡張を実行する前に確認ダイアログを表示する。誤操作による費用支払いを防ぐ。

### 5. 倉庫のアイテムを一括で売却できるようにする

> 倉庫のアイテムを一括で売却できるようにする。

倉庫のアイテムをまとめて売却できるようにする。

**補足**: 装備中・ロック中の装備を誤って売らない保護が必須。一括対象の選択方法（全選択 / 種別で絞る / 個別チェック）を決めること。

### 6. アイテムの詳細を倉庫でカーソルを合わせると表示できるようにする

> アイテムの詳細を倉庫でカーソルを合わせると表示できるようにする。

倉庫の一覧でホバー時にアイテム詳細をツールチップ表示する。

### 10. 消耗品スロットと装備は枠をクリックして、装備できるものの中から選ぶようにする

> 消耗品スロットと装備は枠をクリックして、装備できるものの中から選ぶようにする。

現在の操作方式を、スロット枠をクリック → 装備可能な候補から選択、という方式へ変更する。

### 11. 装備は別の保管用枠を用意する

> 装備は別の保管用枠を用意する。

装備を通常アイテムとは別の保管枠で管理する。

**補足**: 倉庫容量の計算方法に影響する可能性がある。装備枠を容量に含めるか別枠にするかで `ProgressionManager` / `MerchantInventory` の容量判定とセーブ構造に波及するため、着手前に方針を確定すること。

---

## D. 市場・鍛冶屋

### 9. 鍛冶屋を奥に進むほど商品が増える形にする

> 鍛冶屋を奥に進むほど商品が増える形する。

鍛冶屋に段階（奥へ進む）の概念を持たせ、進むほど扱う商品が増える構成にする。

**補足**: 「奥に進む」の解釈（階層UI / 解放段階 / 商人レベル連動など）が複数あり得るため、着手前にユーザーへ確認すること。既存の町別ランク帯設定との整合も要確認。

### 12. 鍛冶屋や市場でも画像を表示・確認できるようにする

> 鍛冶屋や市場でも画像が確認を表示するようにする。

鍛冶屋・市場の商品一覧でもアイテム画像を表示する。

**補足**: 原文は誤字を含むため上記の解釈で記録。図鑑用画像（`Resources/UI/Codex/Equipment/`）の流用可否を確認すること。未配置画像がある場合の代替表示も必要。

---

## E. 表示・演出

### 8. 1日のリザルトの文字に色を付けてわかりやすくする

> １日のリザルトの文字に色を付けてわかりやすくする。

日次リザルトの文字を色分けし、収支や増減を読み取りやすくする。

**補足**: 収入/支出、増加/減少などの意味に色を対応させる。既存の `UITheme` に色定義を寄せると重複を避けられる。

### 14. 町マップの画像を町ごとに変更する

> 町マップの画像を町ごとに変更する。

現在共通の町マップ画像を、町ごとに個別の画像へ差し替える。

**補足**: 画像アセットの新規制作が必要。着手時は `docs/IMAGE_ASSET_LIST.md` へ追記すること。町数分の画像が必要になるため、大量画像生成は着手前にユーザー承認を得る運用に従うこと。

---

## F. 図鑑・メニュー

### 16. 装備以外のアイテムの図鑑をメニューに追加

> 装備以外のアイテムの図鑑とタイトル画面に戻るボタンをメニューに追加

現在は装備図鑑と魔物図鑑のみ。素材・消耗品など装備以外のアイテムの図鑑を追加し、メニューから開けるようにする。

**補足**: 既存の共通ブックUI `BookPageUI`（羊皮紙見開き・1見開き4件・ページ送り・未発見は「？？？」）を流用できる。ただし `BookPageUI` は装備図鑑と魔物図鑑で共用しているため、**改変せず兄弟コンポーネントとして追加する**方針を踏襲すること（装備の特殊ページ実装時と同じ判断）。

**発見管理の要確認事項**: 装備は `MerchantInventory.HasDiscoveredEquipment`（セーブ対象の `discoveredEquipmentPersistentIds`）、魔物は `MonsterCodexManager`（セーブ対象の `encounteredEnemyIds`、セーブv25で追加）で発見状態を管理している。**アイテム用の発見管理が既存に無い場合はセーブ構造の追加が必要**で、`CurrentVersion`（現在28）の更新とマイグレーション追加を伴う。着手前に既存の発見管理を再利用できるか確認すること。

**画像**: アイテム画像の配置規約と未配置時の代替表示を確認すること。図鑑用画像が新規に必要なら `docs/IMAGE_ASSET_LIST.md` へ追記する。

### 17. タイトル画面に戻るボタンをメニューに追加

> 装備以外のアイテムの図鑑とタイトル画面に戻るボタンをメニューに追加

メニューからタイトル画面（`Title` シーン）へ戻れるボタンを追加する。

**補足**: タイトルは専用シーンに分離済み（`Assets/Proiject/Scenes/Title.unity`）なので、シーン遷移自体は `TitleSceneController` の逆方向を実装すればよい。**戻る前にセーブを確定させること**、および誤操作防止の確認ダイアログ（#4 の倉庫拡張確認UIと同種）を併せて検討すること。戦闘中・ダンジョン探索中・輸送/遠征の進行中に戻れてしまう場合の状態整合も要確認。

---

## 着手にあたっての注意

- **提出前の優先順位**: 現在は提出前検証フェーズ（ビルド確認・手動確認・スクリーンショット/動画）が先。本バックログは提出後または並行して余力がある場合に着手する。
- **セーブ影響の事前確認**: #11（装備の保管枠）、#15（初回訪問フラグ）、#16（アイテムの発見管理）はセーブ構造へ波及する可能性がある。着手前に `GameSaveData.CurrentVersion`（現在28）とマイグレーションの要否を判定すること。
- **ユーザー確認が必要な項目**: #1 と #15 の方式選択、#9 の「奥に進む」の具体化、#11 の容量計算方針。
- 各作業後に `dotnet build DungeonMerchant.sln --no-restore` で警告0・エラー0を確認し、新規 `.cs` は csproj 登録、新規 `.meta` の GUID 重複0を守ること。
