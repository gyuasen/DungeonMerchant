# DungeonMerchant 構造評価

このプロジェクトのアーキテクチャを、良い点・悪い点を同じ密度で評価する。
初回評価日：2026-07-30 / **改訂日：2026-07-30（改善作業後）**
対象：Assets/Proiject（Scripts 152ファイル、Tests 75ファイル）

**評価方針**：印象ではなく実測に基づく。「失敗が多いはず」という前提も検証対象とする——実際に測った結果、その前提は**部分的にしか正しくなかった**。

> **改訂について**：初回評価の後、UI層とパフォーマンスの改善を実施した。本文は初回評価の内容を保持し、変化した箇所には「改善後」を併記する。**評価が下された時点の状態と、それに対して何をしたかの両方を残すため**、初回評価を書き換えて消すことはしない。

---

## 総合評価

**個人開発のゲームプロジェクトとしては、平均を明確に上回る。**

構造的な弱点は確かに存在し、そのうち1つ（UI層の肥大）は深刻。ただし**弱点の性質が「よくある失敗」とは違う**。典型的な個人開発の失敗は「動けばいい、テストなし、後から手が付けられない」だが、このプロジェクトは逆方向に振れている——**テストと分離への投資は実在し、機能している**。問題は投資が**UI層に届かなかった**ことであり、無かったことではない。

| 領域 | 初回 | **最終** | 根拠 |
|---|---|---|---|
| テスト戦略 | A | **A** | 713→**797件**/13,982行。対象選定が的確 |
| ドメインロジックの分離 | B+ | **B+** | 24サービス抽出、38ファイルがUnity完全非依存 |
| セーブ設計 | B+ | **A−** | v37まで移行を維持、永続ID採用、原子的書き込み |
| パフォーマンス対応 | B | **B+** | 計測起点。**保存経路の全走査を除去し構造対処へ前進** |
| 依存関係管理 | C | **B−** | FindObjectOfType 134→**101**、`??`解決を全廃、**Domainアセンブリでコンパイラ強制** |
| UI層の構造 | **D** | **B** | partial 19→**12**、**12クラス抽出**、UI層テスト**116件** |

> 各領域の詳細な改訂経緯は本文中の「改善後」節および末尾の改訂履歴を参照。以下の本文は初回評価時点の記述を保持している。

**最終的に、弱点だった2領域（UI層 D→B、依存管理 C→B−）が大きく改善し、全領域がB−以上に到達した。** 詳細は以下の改訂履歴を参照。

### 総合評価（2026-07-31 最終）

**個人開発のUnityプロジェクトとして、上位に位置する。** 特にテスト戦略（797件・パリティテスト・回帰テストの運用）とセーブ設計（v37まで移行維持）は、個人制作の水準を明確に超えている。

弱点だったUI層も、19partialの巨大クラスから12クラスを抽出し、カプセル化の境界を実際に作った。**「問題を認識し、実測で優先順位を決め、低リスクから段階的に解消する」という一連のプロセスが記録として残っている**ことが、静的な品質以上の価値を持つ。

Aに届かない理由は明確で、`SimpleMercenaryHireUI` が依然9,598行あり、アセンブリ境界の強制範囲が2ファイルに留まること。**方向は正しく、到達距離が残っている**という初回評価時の総括は、規模を縮めつつ現在も当てはまる。

> **第2回改訂（2026-07-31）**：追加作業により数値がさらに更新された。
> - EditModeテスト **735 → 793件**（PlayMode 8件と合わせ**全801件・失敗0**。PlayModeも再実証済み）
> - UI層テスト **58 → 116件**。未テストだった2大Controller（`CharacterEquipmentController` 882行、`TownTravelController` 667行）に計58件を追加し、**抽出済みControllerで主要なものはすべてテストを持つ状態**になった
> - 共有フィールド **171 → 162個**（累計 317 → 162、**49%減**）。グルーピングは主要な群をすべて処理し完了
>
> UI層の評価は **C → C+** に引き上げる。「テストできる構造にしたのにテストが無い」という不整合が解消されたため。Bに届かない理由（10,406行の単一クラス）は変わらない。

> **第3回改訂（2026-07-31）**：アセンブリ分割の最小実証を実施。依存管理は **C+ → B−**。
> - `DungeonMerchant.Domain`（`noEngineReferences: true`・references空・autoReferenced無効）を新設し、依存が完全に閉じた凝集単位（傭兵コスト計算2ファイル）を分離。「Unity非依存」が**初めてコンパイラ強制**になった
> - **負の実証済み**：Domain側に `UnityEngine.Mathf` 参照を書くと CS0103 でコンパイル拒否されることを確認
> - 候補選定で `TownServicePolicy` は**見送り**（`WorldMapService` 経由で `ItemDataSO` に届き閉包が閉じない）。移動はコンパイルで閉包を保証できる範囲に限定した
> - Bに届かない理由：強制境界の中身はまだ2ファイル。残り30超の純粋C#候補は規約のまま
>
> あわせてEditor実行のみで発生するテスト汚染（破棄済み `SaveManager` のゾンビ購読）を診断・修正し、回帰テストを追加。EditMode **794件**・PlayMode 8件、失敗0。診断はCodex(Sol)の監査、実装はCodex(Terra)、検証は本セッションの分担で実施した。

> **第4回改訂（2026-07-31 最終）**：partial解体を実施し、**UI層を D → B** とする。
> - `SimpleMercenaryHireUI` の partial を **19本 → 12本**へ。9機能（Tutorial / ContractDetails / FacilityGreeting / MonsterCodex / Onboarding / TrainingGround / RemoteSale / Story / DailyResult）を独立クラスへ変換した
> - 抽出した **12クラス**はいずれも `SimpleMercenaryHireUI` への参照・`GetComponent`・`FindObjectOfType` を持たず、依存はコンストラクタ注入のみ。**これらの機能のフィールドはコンパイラ上も真に private** になった
> - Story は `StartCoroutine` と `SceneManager.LoadScene` を、DailyResult は日次調停とキュー状態を本体に残し、**表示生成のみを抽出**。非MonoBehaviourの制約と横断的関心を踏まえた線引き
> - 別環境（学校側）が同じ規約に従って新機能（アイテム図鑑）を `ItemCodexOverlayView` + `ItemCodexPresenter` として追加。**確立したパターンが第三者に再現された**
>
> 最終実測：EditMode **797件**・PlayMode **8件**、失敗0。partial 12本・合計 9,598行。
> **Bに留めAとしない理由**：本体は依然 9,598行あり、共有フィールドも154個残る。`Map`(956行) `HireParty`(978行) `CharacterEquipment`(1,330行) `BattleDungeon`(1,209行) `Economy`(1,030行) は未変換。**構造の方向は正しいが到達距離が残っている**。

---

## 実測データ

| 指標 | 初回実測 | **改善後** |
|---|---|---|
| Scripts 総ファイル数 | 152（MonoBehaviour 36 = 24%） | 152（変化なし） |
| **Unity完全非依存ファイル** | **38（25%）** | 38（変化なし） |
| 抽出済みサービスクラス | 23 | 23（変化なし） |
| テスト属性数 | **713** | **735**（+22） |
| テストコード行数 | 12,096行 | 12,489行 |
| テストコード比率 | 29% | **30%** |
| UI層のテスト件数 | （未計測） | **58件 / 18ファイル** |
| `FindObjectOfType` | 134箇所 / 30ファイル | **101箇所**（△33 / **25%減**） |
| `??` によるfake-null素通り解決 | 6ファイル | **0**（全廃） |
| Runtime asmdef | 1枚 | 1枚（**未着手**） |
| `SimpleMercenaryHireUI` 合計 | **10,128行** | 10,406行（**ほぼ未変化**） |
| **同クラスの共有フィールド** | **317** | **171**（△146 / **46%減**） |
| UIフォルダ | 21,268行 | 21,546行 |

**行数が微増している点は隠さない。** References クラス17個と、その `IsValid` を追加したため。**行数削減は目的ではなく、カプセル化の境界を作ることが目的**だった。

---

## 評価できる点

### 1. テスト戦略 — このプロジェクト最大の強み【A】

**713件のテスト、12,096行**（改善後：**735件 / 12,489行**）。プロダクションコードに対して**29%のテストコード比率**は、個人開発では極めて高い。

ただし数より重要なのは**対象選定**。上位ファイルを見ると、テストが「書きやすい所」ではなく「壊れると痛い所」に置かれている：

| テストファイル | 件数 | 守っている対象 |
|---|---|---|
| `TownAvailabilityParityTests` | **58** | リファクタリング前後の等価性 |
| `BattleSkillResolverTests` | 42 | 戦闘スキル解決（1,120行の複雑ロジック） |
| `DungeonEquipmentSetBonusTests` | 27 | セット装備ボーナス（仕様が入り組む） |
| `WorldMapServiceTests` | 24 | マップ解放判定（セーブ復元と絡む） |
| `BalanceExpansionDefinitionTests` | 22 | バランス定義の整合 |

**特筆すべきは `TownAvailabilityParityTests` の58件**。これは機能テストではなく**パリティテスト**——リファクタリング前の実装と後の実装が、810通りの組み合わせで同じ結果を返すことを検証している。

これは「テストを書いた」のではなく「**安全にリファクタリングするための足場を組んだ**」ということ。手段と目的の関係を理解している証拠であり、実務のレガシー改修で最も求められる考え方に一致する。

**加えて、テストが実際に重大バグを捕まえた実績がある**。初期借金5000万への変更後、`5000万 × 75` が int を溢れて返済判定が壊れた件（Q2-3）。これは**後発の変更が既存ロジックを壊す**類のバグで、単体のコードレビューではまず見つからない。回帰テストが継続的に回っていたから捕捉できた。**テスト投資が具体的に回収された事例**として明確。

### 2. ドメインロジックの分離は「掛け声」ではなく実在する【B+】

前回の文書（REBUILD_RETROSPECTIVE）で「Unity非依存層は規約でしかなく強制力がない」と書いたが、**これは公平性を欠いていた**。強制力がないのは事実だが、**規約は実際に守られている**。

実測：**152ファイル中38ファイル（25%）が `MonoBehaviour` も `using UnityEngine` も含まない完全な純粋C#**。

フォルダ別の純度：

| フォルダ | 純粋 / 総数 | 評価 |
|---|---|---|
| Item | 8 / 17 (47%) | 良好。値オブジェクトが適切に分離 |
| Battle | 5 / 15 (33%) | 良好。`DamageResolver` `BattleStatusEffectService` など |
| Core | 3 / 14 (21%) | `WorldMapService` `GameSaveData` は分離済み |
| Mercenary | 3 / 14 (21%) | `HealingCostService` `TrainingCostService` |
| **UI** | **14 / 66 (21%)** | UIですら2割が純粋（`BattlePageUI` 等） |

`EquipmentQuality` `ItemRarity` `EquipmentSlot` のような列挙・値型がUnityから独立し、`HealingCostService` `TrainingCostService` `TownServicePolicy` のような**料金・ポリシー計算が純粋関数として切り出されている**。これは意識的な設計判断の結果であり、偶然ではない。

**23個のサービスクラス**（`*Service` `*Calculator` `*Resolver` `*Builder` `*Formatter` `*Policy`）が抽出されている点も、責務分離への継続的な取り組みを示す。

### 3. セーブ設計 — 個人開発として異例の水準【B+】

version **36** まで後方互換を維持。個人開発なら「セーブを消してくれ」で済ませられる場面を、36回すべて移行コードで対応している。

構造も適切：

- 移行が機能単位の関数に分割（`MigrateDebt` `MigrateQuestLocations` `MigrateStoryProgress` …）
- 各関数が `sourceVersion` を見て自分の担当だけ処理 → 新規追加時に既存を読まなくてよい
- **永続IDによる参照解決** → アセット再編成とセーブ互換を切り離した
- `version > CurrentVersion` はそのまま返す → 新版データを旧版が破壊しない

最後の1点は**見落とされやすい配慮**で、ここまで手が回っているのは評価に値する。

### 4. パフォーマンス対応が計測起点である【B】

3件の最適化コミットすべてが「推測ではなく計測」から始まっている。特にボス戦フリーズの件は、**症状（戦闘中）と原因（倉庫UIの `FindObjectOfType` ループ）が全く別の場所**だった。推測で戦闘コードを触っていれば時間を浪費していたところを、Profilerで実測して9行の修正で解決している。

`BattleRewardService` の最適化で**乱数消費順の不変性を保証**した点も見識が高い。ループの回し方を変えると乱数の引き順が変わり、同じシードで結果が変わる——この落とし穴を認識し、「観測可能な振る舞いを1ミリも変えない」を最適化の前提条件として守っている。

**Bに留めた理由**は後述（対症療法に留まっている）。

---

## 問題点

### 1. UI層の肥大 — 最も深刻な構造的欠陥【D】

`SimpleMercenaryHireUI` は19ファイルにpartial分割されているが、**合計10,128行**。分割は物理的なものに留まり、**論理的には依然として単一の巨大クラス**。

決定的な問題は**共有フィールド/メソッド宣言が271個**あること。partial は同一クラスなので、これら271個の状態が**19ファイル全域から無制限にアクセス可能**。つまり：

- `SimpleMercenaryHireUI.Economy.cs` を読むだけでは、そこで触る状態が他のどこから書き換えられるか分からない
- カプセル化の境界が存在しない（`private` が事実上 `internal` として機能している）
- 1つの状態変更の影響範囲が10,128行全体

**UIフォルダはScripts全体の51%（21,268行）**を占め、ロジックがUI層に引き寄せられ続けた結果を示している。

**ただし公平を期すと**：ここには「巨大クラスを解体しようとした形跡」が明確にある。`EconomyController` `CharacterEquipmentController` `DailyResultController` などのController抽出は実際に進んでおり、抽出済みクラスは**コンストラクタ注入で依存を明示している**（`EconomyController` は10個の依存を受け取る）。方向は正しく、**到達距離が足りていない**というのが正確な評価。

#### 改善後：D → C

共有フィールドを機能ごとの `*References` クラス17個へ束ね、**317個 → 171個（46%減）**とした。

| 何が変わったか | 効果 |
|---|---|
| 無制限に見える状態が146個減った | 状態変更の影響範囲が「10,406行全体」から機能単位に縮んだ |
| 参照が型でグループ化された | `tutorial.closeButton` と書けば所属機能が一目で分かる |
| `IsValid` が参照漏れを検出 | 手書きの5連null判定が `tutorial.IsValid` に置き換わった |
| UI層テストが58件（18ファイル） | `EconomyController` は5→27件 |

**Bに届かない理由**：`SimpleMercenaryHireUI` は依然**10,406行の単一クラス**であり、partialによる物理分割のままである点は変わっていない。残る171個の共有フィールドも、依然として19ファイル全域から見える。**カプセル化の境界は生まれたが、クラスそのものは分割されていない。**

### 2. 依存関係が型検索で解決されている【C】

`FindObjectOfType` が **134箇所 / 30ファイル**。

| ファイル | 件数 |
|---|---|
| `SimpleMercenaryHireUI.cs` | 20 |
| `SaveManager.cs` | **18** |
| `ProgressionManager.cs` | 8 |
| `OnboardingGuideController.cs` | 7 |

**`SaveManager.cs` の18箇所が構造的に最も問題**。セーブはドメイン状態の永続化であり、本来シーン構成から独立すべき層。そこがシーン上のオブジェクトを型検索で掴んでいるということは、**セーブ処理がシーン構成に暗黙依存している**。

問題は性能だけではない：

1. **依存が不可視** — クラス定義を見ても何に依存するか分からず、全メソッドを読む必要がある
2. **テスト不能** — シーンが必要になり、EditModeテストに持ち込めない
3. **fake-null 事故** — `StoryProgressManager` の購読漏れ（明示注入で対処済み）は暗黙解決が原因

そしてQ3-1のフリーズは、この134箇所の**うち1箇所**が原因だった。**残り133箇所は同種のリスクを抱えたまま**。この事実が、パフォーマンス対応をBに留めた理由——**個別の症状は正しく直したが、症状を生む構造は残っている**。

#### 改善後：C → C+（パフォーマンス対応は B → B+）

**134箇所 → 101箇所（25%減）**。ただし数の削減以上に重要なのは、**何が問題だったかの認識が実測で覆った**こと。

当初は「`SimpleMercenaryHireUI.cs` の20箇所」が最大の問題だと考えていたが、実測するとそれらは `Start()` から一度だけ呼ばれ、`== null` で正しくガードされていた。**性能上の問題は無かった。**

真の問題は `SaveManager` にあった——**ガードが一切無く、`SaveGame()` のたびに18回シーン全走査**していた。保存は装備変更・雇用・町移動・物語節目のたびに走るため、プレイ中に繰り返し発生していた。

さらに5ファイルで `??` による参照解決が見つかった。Unity の未設定 `SerializeField` や破棄済みコンポーネントは **C#参照としては非nullな "fake null"** であり、`??` はこれを素通りさせる。結果、未解決の参照が永久に解決されず走査し続ける。`ProgressionManager` の場合、`CanStore`（アイテム追加のたびに呼ばれる）が経路だった。

6ファイルに `Resolve<T>(ref T)` を導入し、**`??` による参照解決を全廃**した。

**Bに届かない理由**：残る101箇所のうち、`HealingManager` `TrainingGroundManager` などは既に正しくガードされており実害は無いが、**依存が不可視でテストできない**という設計上の問題は残っている。asmdef 1枚という根本原因も未着手。

### 3. アセンブリ境界の不在【C の根本原因】

Runtime asmdef が **1枚**。全152ファイルが単一アセンブリに同居し、`Core` → `UI` の参照も、その逆も、コンパイラは一切警告しない。

前述の通り、分離の規約は**実際には守られている**（純粋C# 25%）。しかしそれは**開発者の注意力のみで支えられている**。`SaveManager` の18箇所が示すように、注意力は一箇所で切れれば崩れる。

`Domain` アセンブリを切って `"noEngineReferences": true` を立てれば、`UnityEngine` を参照した瞬間コンパイルエラーになる——**規約が物理的制約に変わる**。現状はその安全装置が無い。

### 4. バランス数値の散在【中程度】

`Balance` フォルダに**1ファイルのみ**。数値の大半がコード内リテラルか個別ScriptableObjectに散在している。

この構造が借金額変更（1億→5000万）の修正コストを押し上げた。連動してチュートリアル文言・物語進行閾値・別動隊の収益前提まで波及している（コミット履歴に残存）。

**「この数値を変えたら何が壊れるか」が把握できない構造**になっている。

---

## 評価の要点：この構造をどう解釈すべきか

### 失敗の性質が「よくある失敗」とは異なる

個人開発の典型的な失敗は**「テストなし・分離なし・後から手が付かない」**。このプロジェクトは違う。

- テスト713件は実在し、パリティテストという高度な使い方までしている
- 純粋C# 25%、サービス23個の抽出は意識的な設計の産物
- セーブ移行v36は多くの商用個人開発より丁寧

**問題は「やらなかった」ではなく「UI層に届かなかった」**。ドメイン層（Battle, Item, Mercenary）には分離とテストが行き渡っている一方、UI層だけが10,128行の塊として残った。

### なぜUI層だけ取り残されたか

構造的な理由がある。**UIはUnity APIとの結合が最も強く、切り出すコストが最も高い**。ドメインロジックは純粋関数にすればテストできるが、UIは `Button` `RectTransform` に触れる限りテストできない。

さらにこのプロジェクトはUI構築を全てコードで行っている（`new GameObject`/`AddComponent` が248箇所、Prefabsフォルダは空）。これは**Unityのシーン/Prefabがマージ困難というリアルな問題への対処**でもあり、一概に誤りとは言えない。ただし結果としてUI層に大量のコードが集中した。

### 開発プロセスとしての評価

コミット履歴（113件）を見ると、**「動くものを作る → 痛みが出た箇所を直す」というサイクルが一貫している**。仕様が固まらない個人開発では合理的な進め方であり、実際に借金額・物語進行・別動隊など仕様は何度も変わっている。先に構造を固めていれば作り直しになっていた。

**反省点があるとすれば、「痛みが出てから直す」を続けた結果、直すタイミングを一度も意識的に作らなかったこと。** `FindObjectOfType` が134箇所まで増えたのは、1箇所ずつ足す判断が毎回は正しくても、全体として管理されなかったから。

---

## 就活での提示方法

**この構造は隠すべきものではなく、説明できれば強い材料になる。**

面接で「設計の弱点は？」と聞かれたときの推奨回答：

> 最大の弱点はUI層です。`SimpleMercenaryHireUI` は19ファイルにpartial分割していますが合計1万行を超えており、論理的には巨大クラスのままです。
>
> 自己評価した時点では共有フィールドが317個あり、partialは同一クラスなので**19ファイル全域から無制限に見える**状態でした。そこを改善し、機能ごとの References クラス17個へ束ねて171個まで減らしています。ただし**クラス自体は分割できていない**ので、評価としてはDからCに上がった程度だと考えています。
>
> 一方でドメイン層は分離できていて、152ファイル中38ファイルがUnity完全非依存、テストは735件あります。**同じ努力をしたのに層によって結果が違った**のは、UIがUnity APIとの結合が強く切り出しコストが高いからです。
>
> 学んだのは、**アセンブリ分割のような「構造を強制する仕組み」を先に入れておくべきだった**ということです。規約は実際25%の純粋C#として守られましたが、`SaveManager` に `FindObjectOfType` が18箇所入り込んだように、注意力だけでは一箇所で崩れます。

この答え方なら、**弱点を認識していること**と**原因を構造的に説明できること**、さらに**手を打った上で残りを正確に把握していること**が同時に伝わる。

### 改善作業そのものが語れる材料になる

「弱点をどう改善したか」を聞かれた場合は、**優先順位を実測で決めた**点を話す。

> UI層の10,128行を全部解体するのは現実的でないので、**評価を下げている要因を分解**しました。実測すると共有フィールド317個のうち200個が単なるUI参照で、ロジックではなかった。**リスクゼロで機械的に整理できる部分が63%を占めていた**ので、そこから着手しています。
>
> ただ、この計画自体にも誤りがありました。`FindObjectOfType` の削減では「呼び出し箇所が20個ある `SimpleMercenaryHireUI`」を最優先にしていたのですが、実測すると**それらは起動時に一度しか実行されず問題ではなかった**。真の問題はガードの無い `SaveManager` で、保存のたびに18回シーン全走査していました。
>
> **「何箇所あるか」ではなく「どれだけの頻度で実行されるか」で優先順位を決めるべきだった**というのが、この作業で得た一番の学びです。

---

## 結論

**個人開発として明確に平均以上。技術的な判断力を示す証拠が複数ある。**

- テスト713件（改善後735件）とパリティテストの発想は、実務のレガシー改修で最も価値ある能力に直結
- 計測起点のパフォーマンス対応、乱数消費順の保存は見識の高さを示す
- セーブ移行v36は運用視点を持っている証拠

**同時に、UI層の1万行超と `FindObjectOfType` 101箇所は実在する負債であり、擁護すべきではない。**

ただしこの負債は**「技術力の不足」ではなく「投資配分の偏り」の結果**。ドメイン層に投資できた事実が、UI層にも同じことができた可能性を示している。実際、Controller抽出は始まっており方向は正しい。到達していないだけ。

**「多くの失敗がある」という当初の見立ては、実測の結果、正確ではなかった。** 失敗は多くはなく、**深い失敗が1箇所（UI層）と、構造的な脆弱性が1箇所（依存管理）**。他の領域は個人開発の水準を上回っている。

---

## 改善作業を経ての結論（2026-07-30 改訂）

**弱点2領域が1段階ずつ改善した（UI層 D→C、依存管理 C→C+、パフォーマンス B→B+）。** ただし**総合評価の性質は変わっていない**。

### 何が変わり、何が変わらなかったか

| | 変わった | 変わらない |
|---|---|---|
| UI層 | 共有フィールド46%減、カプセル化の境界が生まれた | **10,406行の単一クラスのまま** |
| 依存管理 | `FindObjectOfType` 25%減、`??`全廃、保存経路の全走査を除去 | **asmdef 1枚のまま** |

**両領域とも、最上位の構造課題には手を付けていない。** これは能力ではなく判断の結果——アセンブリ分割もクラス分割も回帰リスクが高く、就活の提出期限を控えた状況では**「低リスクで測定可能な改善」を優先した**。

### この改訂で最も価値のある発見

**計画そのものが実測で否定された。** `FindObjectOfType` の削減計画は「箇所数が多い順」で優先度を決めていたが、実際には**呼び出し頻度こそが問題**であり、最優先としていた20箇所は実害が無かった。

これはQ3-1（ボス戦フリーズ：症状は戦闘、原因は倉庫UI）と**同じ構図が計画レベルで再現した**もの。「推測ではなく計測」という原則は、コードだけでなく**改善計画にも適用すべきだった**。

この失敗は隠すべきものではなく、**測ることで自分の想定を修正できた事例**として提示できる。
